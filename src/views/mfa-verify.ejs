<!-- src/views/mfa-verify.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verify MFA | Mountain Auth</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <div class="card">
      <div class="card-header">
        <h1 class="title">Verify MFA</h1>
      </div>
      
      <div class="card-body">
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="alert alert-danger"><%= error %></div>
        <% } %>
        
        <p style="text-align: center; margin-bottom: 1.5rem;">
          Please enter the verification code from your authenticator app for user <strong><%= username %></strong>.
        </p>
        
        <form action="/auth/mfa/verify" method="POST">
          <div class="form-group">
            <label for="token">Verification Code</label>
            <input 
              type="text" 
              id="token" 
              name="token" 
              class="form-control" 
              placeholder="Enter 6-digit code" 
              required 
              autofocus
              pattern="[0-9]{6}"
              inputmode="numeric"
              autocomplete="one-time-code"
            >
          </div>
          
          <div class="form-group">
            <button type="submit" class="btn btn-primary">Verify</button>
          </div>
        </form>
      </div>
      
      <div class="card-footer">
        <p><a href="/auth/login">Back to Login</a></p>
      </div>
    </div>
  </div>
</body>
</html>